# 🤖 Lightweight Code Agent

A minimalist, Python-based CLI code assistant inspired by Claude Code. This agent provides a streamlined terminal interface for rapid development.

> **Note:** This is a lightweight alternative. It focuses on speed and simplicity rather than the full feature set of Claude Code.

## 🚀 Quick Start

### 1. Prerequisites
Ensure you have Python installed, then install the required dependencies:

```bash
pip install rich prompt_toolkit requests
